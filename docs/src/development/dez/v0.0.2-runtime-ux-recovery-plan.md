# Dez v0.0.2 Runtime UX Recovery Plan

This plan records what the installed Apple-silicon build actually does and
orders the work required before another public preview. It refines
[v0.0.2 Active Plan](./v0.0.2-active-plan.md); it does not replace
[Fork Notes](./fork-notes.md).

## Product decision {#product-decision}

Dez is a terminal-first code review IDE:

> Run work in a terminal, supervise its state in the Session Rail, and inspect
> or change the result with the editor, Files, Search, Git, diffs, diagnostics,
> Debug, and Agent without leaving the Workspace.

The terminal is where computation runs. It is not the whole product. The IDE is
the inspection and intervention layer around that computation.

The release-defining loop is:

```text
Open Workspace
-> Start Terminal Session
-> Run Codex or another long-lived command
-> See one truthful Session Rail row
-> Open changed files, Git, or a Review Brief
-> Inspect or edit in the Main Work Area
-> Return to the same live Terminal Session
```

Agent Sessions are a second way to delegate work through a native conversation.
They are not terminals, and Agent History is not Git history.

## Installed runtime audited {#installed-runtime-audited}

The audit used the exact installed application rather than a source preview:

| Field             | Observed value                             |
| ----------------- | ------------------------------------------ |
| Application       | `/Applications/Dez.app`                    |
| Version           | `0.0.2+dev.3`                              |
| Source checkpoint | `91a1514f0236b7d63c946431b08a00114e41f4d4` |
| Bundle identity   | `dev.dez.Dez`                              |
| Platform          | macOS, Apple silicon                       |
| Audit date        | 2026-07-24                                 |

The downloaded DMG remained mounted at `/Volumes/Dez`. The log confirms that
Dez first ran from the mounted image and later ran from `/Applications`. The
running process now belongs to the installed copy, but both bundles advertise
the same identity to macOS.

The audit inspected the welcome window, a Workspace with a Codex terminal, the
Session Rail, the Git Changes tool, an unavailable restored terminal, process
ownership, and recent application logs. It did not start a second Dez or Zed
binary.

## What the runtime currently communicates {#current-communication}

The implementation has the right underlying pieces but does not present one
understandable workflow.

| Priority | Runtime observation                                                                                                             | Why it breaks the product                                                                                       |
| -------- | ------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------- |
| P0       | A running terminal appeared beside **No sessions yet**. A later restore showed the row.                                         | The supervisory surface contradicts the visible computation.                                                    |
| P0       | A terminal row said **Persistent**, but the installed run had no `dez-terminal-host` process; the shell was a child of the GUI. | The label implies survival across GUI exit that the active ownership path does not provide.                     |
| P0       | Application shutdown logged two `app_will_quit` timeouts, then restarted.                                                       | Close and reopen is the release-defining path; a timed-out quit is not a cosmetic issue.                        |
| P0       | Restoring logged `missing serialized agent thread item`.                                                                        | Saved Agent Session navigation can be incomplete or misleading.                                                 |
| P0       | Focus repeatedly landed on elements with no accessibility node or role.                                                         | Keyboard and assistive navigation can announce the entire window instead of the focused control.                |
| P1       | Opening Git produced three competing columns: Session Rail, a wide Changes tool, and the terminal.                              | The Main Work Area stopped reading as the primary work surface.                                                 |
| P1       | **History** appeared both in the Session Rail footer and beside Git Changes.                                                    | One label refers to Agent History and the other to Git history.                                                 |
| P1       | The terminal and five changed files had no visible relationship or direct review handoff.                                       | The IDE integration exists as separate tools, not as a coherent agent-review loop.                              |
| P1       | The welcome screen says “Start in a terminal” and offers five unrelated actions.                                                | It does not teach Workspace, Session, supervision, or review.                                                   |
| P1       | Files, Git, diffs, diagnostics, Debug, and Agent are hidden behind small controls.                                              | A new user reasonably concludes that Dez is only a terminal with a custom sidebar.                              |
| P2       | Lumin Light leaves large near-white regions with weak pane and title hierarchy.                                                 | The three regions blend into one unfinished canvas.                                                             |
| P2       | The Codex TUI mascot dominates the blank terminal.                                                                              | The mascot is Codex content, not Dez chrome, but the missing session frame makes it look like Dez's primary UI. |
| P2       | The mounted and installed applications coexist after installation.                                                              | Launch Services, automation, and the user can target the wrong copy.                                            |

## Source progress {#source-progress}

The first recovery slice is implemented in source:

- The Dez welcome screen now teaches **Run**, **Supervise**, and **Review** as
  one sequence. **Open Workspace** is primary; a home-directory terminal is
  explicitly **Open Scratch Terminal**.
- Git's ambiguous tabs are now **Git Changes** and **Git History**.
- A terminal is labeled **Persistent** only when an active external terminal
  Host owns its exact saved snapshot. GUI-owned terminals are labeled
  **Workspace Terminal Session**.
- Scratch terminals in an empty Workspace now get a transient Session Rail
  group, so visible computation cannot coexist with **No sessions yet** merely
  because the Workspace has no project path.
- Session Rail now says **Loading sessions** while Workspace restoration or Host
  reconciliation is still pending. Start-fresh actions remain unavailable
  until restoration reaches its ready state. A genuinely empty rail names
  **Open Workspace** as the primary folder-only, same-window route and keeps
  **Open Scratch Terminal** as a secondary pathless option. With a ready
  Workspace but no Sessions, **Start Terminal Session** is the primary action.
  The zero-state Run/Supervise/Review explanation is a passive, named
  icon-and-copy list without button-like containers, so it cannot compete with
  those transitions. Loading presentation takes precedence over stale
  attention styling.
- A stale saved Agent Session tab is now omitted as typed recoverable state
  instead of emitting the generic `missing serialized agent thread item`
  failure. Dez shows one persistent recovery notice pointing to Agent History;
  the next Workspace serialization heals the stale layout.
- Every standalone Dez terminal now has a compact context bar driven by
  terminal, Workspace, and Git owners. It shows lifecycle, Workspace/branch,
  and live changed-file count; **Files**, **Review Changes**, and **Session
  Details** provide the direct IDE handoff. Git changes refresh through
  structured Git-store events rather than terminal text parsing.
- The context bar now gives its fixed action group priority at narrow pane
  widths. Lifecycle/repository metadata is the shrinkable, clipped region; its
  full values remain in Session Details instead of painting through Files,
  Review Changes, or Details.
- **Review Changes** is now one Workspace-owned Git action rather than a pair of
  panel toggles. It reveals the bounded Git Changes drawer, selects a concrete
  changed file, and opens the uncommitted diff in the Main Work Area. Repeating
  it cannot close the drawer accidentally; a clean repository stays on the
  explicit Git clean state instead of opening an empty diff.
- The resulting Main Work Area review now identifies its active Surface as
  **Diff · filename**. Its tooltip retains the diff base and relative path;
  official Zed keeps its inherited Project Diff vocabulary.
- **Files** now uses one Workspace-owned, idempotent reveal/focus action across
  Welcome, the active terminal, Session Rail keyboard path, and terminal
  context menu. Repeating the destination action cannot close Files or expose a
  different Workspace Tool.
- When the selected saved Session belongs to a closed Workspace, **Files** now
  reopens that exact Workspace, restores the existing Agent or Terminal
  Session, and only then reveals the project tree. It no longer fails silently
  or creates a substitute Session.
- Terminal rows expose the same Git review and Session Details vocabulary.
- Starting a recognized agent inside a visible terminal now refreshes that
  terminal Surface immediately as well as Sessions. The tab owns the provider
  icon and the context strip owns the concise **Agent running** state without
  repeating the glyph; no overlay or second agent panel appears. The detail
  disclosure names foreground-process observation as evidence, while an
  ordinary active shell adds no redundant activity label.
- Workspace Tools and Agent panes start at the smaller of 360 px or 22% of
  available horizontal space. Opening, resizing, hiding, restoring, or applying
  a saved layout preserves at least 60% for primary work instead of producing
  equal-width columns. Restored Agent panes retain Agent-region ownership.
- Sessions reserves and renders one responsive 200–280 px width as a normal
  flex sibling of the Main Work Area. In client-decorated Dez windows the rail
  stays in normal flow rather than stretching an absolute surface over the
  reserved region, and its header divider does not add a full-size pointer
  layer. Official Zed retains its inherited decoration geometry.
- Dez's native window floor is 600 × 400 px. At that narrow checkpoint the
  200 px Sessions floor leaves 400 px for the Main Work Area before an optional
  drawer; the inherited 360 px window floor would mathematically reduce primary
  work to 160 px. Official Zed retains its upstream 360 × 240 px floor.
- The Session Switcher is the only Sessions-owned full-window interaction
  boundary. It is an explicit dialog, dismisses on outside click or Escape, and
  contains inside clicks; it cannot leave an invisible surface blocking the
  Main Work Area.
- Single-work-area recipes and ordinary Workspace restoration now hide surplus
  empty tab panes left by earlier split layouts, which removes the unexplained
  blank column observed in the installed artifact. Panes containing files,
  terminals, or other user work remain visible, and explicit multi-pane recipes
  retain their requested empty work areas. The default active-pane cue moves to
  the title/selected tab instead of outlining the entire pane. Dez does not
  paint the inherited whole-pane focus overlay, including when imported
  settings request it; official Zed retains that configurable upstream option.
  The remaining empty Main Work Area is one top-anchored native region
  headed **Run, supervise, and review in this Workspace** with the three
  immediate Workspace actions. It is neither a generic placeholder nor a
  bordered card floating in a blank canvas. Its passive workflow line uses
  plain icon-and-copy groups so only actual actions look interactive.
- Welcome guidance now adapts to real Workspace state. With no Workspace its
  primary section is limited to **Open Workspace**, **Clone Repository**, and
  **Open Scratch Terminal**; inside a Workspace it becomes **Start Terminal
  Session**, **Open Files**, and **New File** instead of asking the user to open
  the Workspace again or promoting generic utilities.
- Welcome now assigns the only filled treatment to that state-dependent first
  action. Secondary choices remain visibly subordinate. Git, Debug, Outline,
  and Agent toolbar controls are keyboard-focusable and specifically named for
  assistive technology; Dez uses Debug and Stop for the debugger's lifecycle
  controls, and its Agent title-edit action remains visible without hover.
- Keyboard focus now reveals the same Workspace-row controls as pointer hover.
  New Terminal and Options remain visible and enter the tab order on a focused
  Workspace. The already-open Workspace menu keeps its scoped close controls
  visible; search clearing and onboarding-banner dismissal are also
  keyboard-focusable.
- Workspace headers no longer insert opaque-window gradient layers between
  their name and action controls. Names truncate in one flexible inline region,
  while Start Terminal Session and Options occupy a fixed sibling region, so
  glass and opaque themes share the same non-overlapping layout. When an
  expanded Workspace has no Sessions, its full labeled Start Terminal Session
  action is the sole creation control; the compact header action remains for
  collapsed or populated Workspaces. The expanded group does not repeat a
  readiness caption already present in the overview and accessible header.
- Main Work Area Back/Forward, Add, Switch Surface, Split, Zoom, and auxiliary
  Hide controls now enter the keyboard tab order. The active unpinned Dez tab
  keeps Close visible and focusable without making every inactive tab noisy;
  pinned dirty tabs preserve their status indicator.
- Visible Dez terminal tabs use the explicit **Terminal · title** surface
  vocabulary when a meaningful title exists. Official Zed keeps its inherited
  tab labels.
- The primary focus roots observed in the installed audit now have stable
  accessibility nodes in source. Populated and empty Files, Outline, Git
  Changes/History, Debug, Agent, native and external Agent Sessions, Agent
  History, and terminal failure/panel surfaces each combine an element ID,
  semantic role, specific label, and their tracked focus handle. Runtime log
  verification remains required against the rebuilt artifact.
- Lumin Blur and Lumin Light now use bounded whole-window glass layers for the
  shell, rail, editor, terminal, toolbar, tab strip, title bar, and status bar.
  The opaque fallback remains available. Dense overlays preserve readability;
  the guard checks alpha ranges, region separation, and a 1.5:1 structural
  boundary after compositing over dark, neutral, cool, and warm backdrops.
- Lumin interaction feedback now follows a measured hierarchy on those same
  surfaces: hover is visible, active/selected are stronger, scrollbars progress
  from idle to hover to drag, and the active editor line has a non-zero floor.
  The opaque fallback no longer makes active weaker than hover.
- JetBrains Mono now spans interface, editor, terminal, prompt, Markdown, Git,
  and review roles by default, so the product identity is visible throughout
  the native IDE rather than only inside code. The exact old Dez-generated
  `.ZedSans` profile upgrades in memory through the normal backed-up settings
  migration; deliberate custom fonts and official Zed profiles are untouched.
- The actual older generated profile found on the installed machine is also
  recognized. Its exact `.ZedSans` + light mode + One Light signature migrates
  as one unit to JetBrains Mono and system-selected Lumin Light/Lumin Blur.
  Arbitrary custom font/theme profiles remain outside this migration.
- Empty ready Workspaces now present **Start Terminal Session** as their filled
  primary recovery and describe live terminal/Agent state without overstating
  the default GUI-owned terminal as durable. Once Sessions exist, each
  Workspace header retains its compact scoped terminal action while the Dez
  overview remains status and scope only, avoiding a second launcher for the
  already-active Workspace.
- Terminal-focused Workspace status now keeps healthy Search and diagnostics
  icon-only while retaining keyboard stops, tooltips, and complete accessible
  labels. Error/warning counts and diagnostic messages remain visible.
- Settings and the command palette now expose **Restore Dez Visual Profile**.
  It repairs only Lumin, JetBrains Mono, and built-in Dez icon choices,
  preserves size/non-visual preferences, and confirms after persistence.
- **Reset Pane Sizes** now preserves Dez's Main Work Area budget instead of
  returning Workspace Tools/Git and the active terminal to equal-width columns.
- Welcome, Onboarding, Session Rail, and terminal-context actions now share one
  icon grammar: Terminal for computation, Folder Open for Workspace/Files,
  File for creation, Diff for review, Info for Session details, List Tree for
  supervision, Clock for Agent History, and Settings for configuration.
- The built-in file/folder icon theme is product-facing as **Dez (Default)**;
  the original **Zed (Default)** name remains available as a compatibility
  alias.
- After a terminal starts, the welcome explanation collapses into the existing
  **Session Details** disclosure. **How Dez Works** preserves the
  **Run → Supervise → Review** model without adding permanent shell chrome.
- The same disclosure now includes a compact **Evidence** contract: lifecycle
  comes from Terminal/Host state, Git counts remain Workspace-owned, change
  attribution is not inferred, agent confidence/checks require trusted
  evidence, and terminal prose is not proof.
- A Scratch Terminal now exposes **Open Workspace** in that same context strip.
  The action accepts folders only and explicitly opens each selected codebase
  in the current window, so the running computation survives the transition
  and gains Files/Git context. Its collapsed guide says **Connect** until that
  context exists.
- Graceful shutdown now gives concurrent PTY cleanup and session-state writes a
  two-second correctness budget instead of racing all observers against a
  200 ms aggregate deadline.
- Official Zed retains its upstream welcome, Git labels, and terminal wording.

This is source evidence only. The installed `91a1514` artifact predates these
changes.

## Target screen model {#target-screen-model}

Dez keeps three visible layers, not three equal columns:

1. **Session Rail**, 264–288 px by default, is the persistent supervisor.
2. **Main Work Area** always owns most of the window and contains terminal,
   editor, diff, search, settings, and review Surfaces.
3. **Workspace Tools** and **Agent** share one optional contextual-drawer slot
   at every window size. Revealing either closes the other, including on an
   ultrawide display. The visible drawer is initially bounded to the smaller of
   360 px or 22% of available width and never silently reduces the Main Work
   Area below 60% of the window.

The active Terminal Session gets one 32 px tab-aligned context header:

```text
[Codex running]     zed 3.0 / main     5 changes
[Files] [Review Changes] [Session Details]
```

For an ordinary active shell, the first segment is omitted. Task failures and
other meaningful states remain visible, and the complete status remains in the
toolbar accessibility label and Session Details.

The action labels disclose progressively instead of appearing as one wide
cluster. Below 480 px, every action remains a keyboard-accessible, tooltip-backed
named icon. From 480 px, the primary transition is labeled: **Review Changes**
when Git reports changes, otherwise **Files** or **Open Workspace**. At 720 px
the secondary Workspace action can join Review, and the long **Session Details**
label appears at 920 px. This preserves one readable IDE handoff in ordinary
split panes without allowing three long labels to displace lifecycle and
repository context at once.

The strip uses authoritative owners:

- the terminal or adapter supplies actor and lifecycle;
- the Workspace supplies repository and branch;
- Git supplies changed-file counts;
- Review Brief discloses what was and was not observed.

It does not parse arbitrary terminal prose into trusted state.

## Ordered implementation {#ordered-implementation}

### Phase 0: restore truth before polish {#phase-0-truth}

1. Make terminal creation publish its Session Rail projection before or in the
   same frame as the terminal Surface. The zero state must not render while a
   visible terminal entry exists.
2. Introduce explicit projection states: **Loading sessions**, populated, and
   truly empty. Do not use **No sessions yet** while restore or host
   reconciliation is pending.
3. Derive durability copy from the active owner:
   - helper-owned and reconnectable: **Persistent**;
   - GUI-owned but retained across tab closure: **Workspace terminal**;
   - saved metadata without a live owner: **Saved** or **Unavailable**.
4. Fix the `app_will_quit` timeout before claiming GUI-independent process
   survival.
5. Repair the missing serialized Agent Session restoration path. A stale record
   must become an explicit unavailable row or be omitted with a recovery notice;
   it must not fail invisibly.
6. Add IDs and roles to every focusable region observed in the runtime log.
   Repeated missing-accessibility-node logs fail the gate.

Primary source owners:

- `sidebar::Sidebar`
- `agent_ui::TerminalThreadMetadataStore`
- `terminal::session_host`
- `workspace::persistence`
- `workspace::MultiWorkspace`

### Phase 1: teach the product in one screen {#phase-1-teach}

Replace the generic welcome action list with a progressive start state.

With no Workspace:

- primary: **Open Workspace**
- secondary: **Clone Repository**
- tertiary: **Open Scratch Terminal**
- explanation: “Open a codebase, run work in a Terminal Session, then review
  its changes here.”

With a Workspace and no Session:

- primary: **Start Terminal Session**
- secondary: **Open Files**
- a passive native icon list: **Run**, **Supervise**, **Review**. It does not
  use a bordered card, dividers, or numbered selection pills.

After the first terminal starts, collapse the guide into a small
**How Dez works** action. Do not permanently consume vertical space.

Primary source owners:

- `workspace::welcome`
- `sidebar::Sidebar`
- first-run settings and persisted onboarding state

### Phase 2: connect the terminal to the IDE {#phase-2-connect}

1. Add the active Session context strip above a detected terminal.
2. Add **Review Changes** when Git reports changes. It opens the Git change list
   and the selected diff in the Main Work Area; it does not merely reveal a
   separate oversized tool column.
3. Add **Files** and **Session Details** beside review. **Session Details**
   contains lifecycle, working directory, Host/Session identity, adapter
   confidence, and explicit setup limitations.
4. Make each Session Rail row expose the same actions through pointer, keyboard,
   context menu, and Command Palette.
5. Keep the terminal one shortcut away while reviewing. Returning must focus the
   existing Surface, never create a duplicate.

Primary source owners:

- `terminal_view::TerminalView`
- `sidebar::Sidebar`
- `git_ui::GitPanel`
- `workspace::Workspace`
- `agent_ui::RunReviewBrief`

### Phase 3: constrain layout and vocabulary {#phase-3-layout}

1. Keep the Session Rail at its compact default unless the user resizes it.
2. Start Workspace Tools and Agent at 360 px or 22% of available width,
   whichever is smaller. Preserve at least 60% for the Main Work Area after
   opening, resizing, hiding, layout recipes, and saved-layout restoration.
3. Restore the previous Main Work Area width when a tool closes.
4. Name tool tabs with their owner:
   - **Git Changes**
   - **Git History**
   - **Agent History**
   - **Files**
   - **Outline**
   - **Debug**
5. Make the active Surface title explicit: **Terminal · Codex**, **Diff ·
   PLAN.md**, or **Editor · file**. Avoid a path-only terminal header.
6. Remove duplicate bottom utility rows. Search and diagnostics belong to the
   Workspace status strip; Agent History and Workspaces belong to the Session
   Rail.

Primary source owners:

- `workspace::PanelItem` and pane sizing
- `git_ui`
- `sidebar`
- `title_bar`
- `status_bar`

### Phase 4: visual and input quality {#phase-4-visual-input}

1. Give Lumin Light and Lumin Blur a visible hierarchy for window background,
   rail, drawer, Main Work Area, tab strip, selection, hover, and focus.
2. Use a 1 px neutral structural boundary with at least 1.5:1 contrast between
   adjacent regions. Reserve the stronger 3:1 non-text floor for focus,
   selection, and interactive state—not every divider.
3. Keep UI labels at the shared readable token. Do not introduce more bespoke
   10–12 px metadata.
4. Test terminal colors independently from surrounding chrome. Dez does not own
   or remove the Codex TUI mascot; it supplies enough surrounding identity that
   terminal content cannot be mistaken for application chrome.
5. Preserve one obvious focus ring and one active Surface. Pointer hover must
   never silently activate another Session.
6. Verify every visible action enters the tab order, has a specific accessible
   name, and reports pressed/open state where relevant.
7. Bound Workspace notifications to the actual Main Work Area: at most 420 px
   wide and 42% tall, with internal scrolling. A toast may occlude only its
   visible content. Full-screen occlusion belongs only to intentional modal
   scrims.

Source token checks enforce hover at 1.18:1, active/selected at 1.3:1, scrollbar
idle/hover/drag at 1.3/1.6/1.9:1, and an active-line floor of 1.09:1 against
their composited surfaces. These are differentiation floors, not text-contrast
substitutes.

### Phase 5: install and restart confidence {#phase-5-install}

1. Make the DMG teach **Drag to Applications**, **Open the installed copy**, and
   **Eject Dez**.
2. When launched from a read-only mounted volume, show one move-to-Applications
   flow before restoring work.
3. Record the running bundle path in diagnostics.
4. Run the exact hero workflow with the external terminal Host enabled and prove
   the shell and foreground process survive GUI exit.
5. If that proof fails, ship without the **Persistent** label and describe
   terminal lifetime honestly.

## Acceptance matrix {#acceptance-matrix}

The next installed artifact does not pass on screenshots alone.

| Scenario              | Required result                                                                                      |
| --------------------- | ---------------------------------------------------------------------------------------------------- |
| First launch from DMG | One install path; no ambiguous parallel running copy.                                                |
| Empty app             | **Open Workspace** is primary and the three-step loop is visible.                                    |
| Start terminal        | A Session Rail row exists no later than the terminal's first visible frame.                          |
| Start Codex           | The row and context strip identify Codex only at the available confidence level.                     |
| Five Git changes      | **Review Changes** opens the change list and a diff without hiding most of the terminal/editor area. |
| Keyboard-only         | Start, select, review, return, rename, detach, and close work with visible focus.                    |
| Accessibility         | No focused control lacks an accessibility node or role; regions and list position are announced.     |
| Close and reopen      | The same process and history return only when helper ownership is active and verified.               |
| Helper unavailable    | The UI says unavailable or workspace-owned; it never says persistent.                                |
| Compact/default/wide  | Main Work Area retains at least 60%; no controls overlap or clip.                                    |
| Lumin Light/Blur      | Region boundaries, text, selection, and focus meet their contrast floors.                            |
| Install cleanup       | The installed app is running and the DMG can be ejected without breaking bundled helpers.            |

Evidence must be captured at four levels:

- source assertions and identity guards;
- focused compilation and tests;
- screenshots plus accessibility trees from the exact artifact;
- restart/process evidence from the installed application.

## Explicit non-goals {#non-goals}

- Do not build a new terminal emulator, Git database, or editor.
- Do not add a dashboard of guessed token counts, costs, or agent state.
- Do not treat raw terminal output as authoritative evidence.
- Do not add more permanent sidebars or global chrome rows.
- Do not expand into remote orchestration before the local run, review, and
  restart loop passes.
- Do not polish false persistence or broken restoration states.

## Latest source-only navigation repair

Detailed Session Rail footers now say **Agent Tools**, **Agent History**, and
**Recent Workspaces**. Default compact widths keep the familiar Settings gear
icon-only while retaining **History** and **Workspaces** labels; genuinely
narrow widths retain accessible, tooltip-backed icons for all three. Runtime
acceptance must confirm all three width states without clipping, wrapping, or
confusion with the Git History surface.

Recognized terminal agents now use one shared provider-icon mapping across the
Session Rail and keyboard session switcher. Runtime acceptance must confirm the
Codex/OpenAI, Claude, Gemini, OpenCode, Grok, Copilot, and Cursor marks remain
legible on both Lumin variants, while unknown providers retain a neutral Robot.

The terminal-to-review handoff now has two additional runtime gates: context
actions must collapse to named, tooltip-backed icons before a narrow Main Work
Area clips, and a Workspace whose active repository is clean but secondary
repository is dirty must switch to that dirty repository and open a real diff.

Canvas Layout command search has one additional runtime gate: the Canvas
Layout command family must contain only Full, Agent Control, Focus Editor,
Code/Run/Observe, Review, Debug, Cycle, Save Layout As, Manage Saved Layouts,
and Restore Previous. The generic centered-editor toggle, legacy mode toggles,
experimental matrices/studios, numeric slots, and clipboard/storage internals
must not appear. Each visible route must perform the named transition without
opening a second drawer.

Git Changes must also prove that its four-line collapsed commit composer leaves
the changed-file list visually primary, that both expansion routes remain
obvious, and that keyboard focus reaches View Diff, staging, commit, remote,
and every split-menu trigger with the correct expanded state.

Agent recovery adds one exact runtime gate: from a window with no Workspace,
**Open Workspace** in Agent must offer folders, retain that same window, and
enable the existing Project-backed IDE surfaces. Keyboard-only acceptance must
reach both Open Workspace and Clone Repository, open and close Agent Options
and New Agent Session, and expose matching focus, selected, and expanded state
without relying on pointer hover.

Agent-provider acceptance must begin with no authenticated non-Zed-cloud
provider. The named setup region must expose keyboard-reachable **Configure
Agent Providers**, never show a Zed sign-in/trial/plan card, and update in place
after configuration. The ready state must expose **Start Agent Session** as a
keyboard-reachable primary action, create exactly one Agent Session, and never
announce that object as a thread.

The Agent composer adds a keyboard-only runtime sequence: expand and restore
the input; open and close Add Context; toggle Follow, Fast, and Thinking where
supported; choose an effort value; send or queue a prompt; stop a running
Agent; and open Sandbox Settings. Focus must stay visible and ordered, disabled
Send must explain the missing input, and every toggle or popup must announce
the same state shown visually.

Agent follow-up acceptance must queue at least three distinct prompts, traverse
their ordered-list announcements, and use Remove, Edit, Steer, and Send Now on
the second row without mutating another entry. Response copy/navigation and
feedback must remain visible and keyboard-operable. A permission request must
exercise Allow, Deny, disabled-Allow recovery, and Permission Scope open,
selection, cancel, and apply paths while preserving the exact tool-call owner.

Agent Review acceptance must change at least three files and use Review, Keep,
and Reject on different rows without hover or cross-file mutation. Pending-edit
decisions must explain why they are disabled. A subagent must return to its
parent without stopping, then stop through the distinct action. Checkpoint
restore must prove Cancel leaves files untouched and Restore Files replaces
only after warning confirmation, while message-edit cancel/restart remain
keyboard-operable.

Agent error acceptance must exercise rate limit, authentication, missing model,
generic retry, skill-load, external-prompt, multi-root, environment, and update
notices. Keyboard focus must reach the specific recovery and dismiss actions;
dismissing one notice must not clear another condition. Copy must announce its
temporary copied state. Data-retention acceptance must prove Learn More and
fallback remain available, Cancel changes no setting and sends no request, and
Accept and Retry persists consent before exactly one retry.

Agent tool-card acceptance must use keyboard and pointer paths on thinking,
terminal, edit, generic tool, and Subagent cards. Copy Code and Copy Command
must be discoverable without hover and announce copied feedback. Each
disclosure must name its content and report expanded state. Stop This Command
must affect only the visible running command; Discard Interrupted Edit must
restore only its exact partial edit. Truncation and exit failure must announce
status without entering the action sequence. Subagent preview must not restart
work, Stop must cancel the exact Subagent, and Open Subagent Session must
navigate to that existing conversation without creating a duplicate.

True-empty Session Rail acceptance must begin without a Workspace and confirm
that the heading, integration copy, Open Workspace, and Open Scratch Terminal
fit at compact/default widths. Keyboard focus must traverse both actions.
Opening a folder must stay in the current window and transition to one
Workspace-ready **Start Terminal Session** action. Starting it must create one
Main Work Area terminal and one matching Session Rail row. Clear Search, Show
All Sessions, Workspace-scoped terminal creation, All, and Attention must
remain reachable in a logical tab order after state transitions.

Recovery-notice acceptance must cover Terminal Session connecting,
reconnecting, and failed states plus one and multiple unresolved Workspaces.
Notices must fit within the bounded scroll region, preserve primary Session
content, and keep Open Local Log, Copy Details/Error, Open Recent Workspaces,
and Remove Recovery Entry keyboard reachable. Copy must match the raw detail,
Recent Workspaces must not claim an automatic retry, removal must affect only
the selected recovery records, and neither path may claim a shell,
reconnection, or Workspace restore that did not occur.

Secondary-window acceptance must open About, Copilot verification, Settings,
Audio Test, and profiling in both Lumin Blur and Lumin Light. Stable windows
must show the active native window material and JetBrains UI role without an
opaque flash or fallback font. Agent attention must remain in Sessions without a
floating popup under the Dez default. Enabling **Floating Attention Popups**
must restore the shaped Agent window with an intentional transparent outer
shell, legible surface contrast, and the configured UI font. Incoming-call and
project-sharing popups retain that material contract. Official Zed keeps its
existing popup policy. This remains a runtime gate; the current source-only
slice does not claim rendered proof.

Sessions-row acceptance must resize the rail through 240, 280, 300, and 380 px
with long Terminal and Agent Session titles. Titles must truncate without a
gradient patch, action overlap, hover jitter that changes row height, or text
painting through controls. A changed Terminal Session must expose one readable
**Review** handoff; a clean Session must expose **Details** instead. At 240 px,
the same actions may collapse to named, tooltip-backed icons. Rename and Codex
hook setup must remain reachable from the Terminal Session context menu without
reappearing as an inline icon cluster. Close or terminate must stay distinct
from the review handoff and must preserve its existing confirmation behavior.
Agent Sessions must expose exactly one inline decision for each state: **Stop**
while running or waiting for permission, **Discard** for a nonempty draft,
**Review** for completed reviewable changes, and **Brief** otherwise when a
review destination exists. Empty drafts may expose no inline action. Rename and
Archive must remain keyboard/context-menu reachable without returning as
secondary hover icons, and a closed Workspace must never show an inert Review
action.

Foreground-process acceptance must start with one existing Main Work Area
shell, launch Codex inside it, return to the shell, and confirm all three states
use the same terminal and Session identity. The Sessions row must change its
provider/evidence on both transitions without a tab switch, duplicate row, or
second terminal. A high-output command must not cause Sessions or Host metadata
to rebuild per PTY frame; selection, scrolling, and terminal input must remain
responsive.

Responsive Sessions acceptance must capture 600, 800, and 1400 px viewport
widths. The rail must reserve and paint the same width—200, 240, and 280 px
respectively under the default compact profile—with no clipping, horizontal
overflow, floating layer, or pointer interception over the Main Work Area.
Closing Sessions must return its reserved width to the Main Work Area, and
reopening it must restore the configured preference subject to the current
viewport cap.

At the 200 px rail floor, scope acceptance must show **All** and **Needs**
without clipping and expose their full session-specific names to assistive
technology; at 240 px **Attention** must return. Truncated overview status must
be recoverable by tooltip. With one optional Workspace Tools or Agent drawer
open, capture 400, 560, 1000, and 2000 px of available workspace. Its initial
width must resolve to 160, 224, 240, and 360 px respectively, its content must
remain operable, and the Main Work Area must never fall below 60% of the
horizontal allocation.
