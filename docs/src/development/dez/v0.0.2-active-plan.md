# Dez v0.0.2 active plan

This document is the active release train for Dez v0.0.2. It turns the
permanent decisions in [Fork Notes](./fork-notes.md) into ordered work and
release gates. It does not override Fork Notes, and it should be replaced when
v0.0.2 ships.

The installed checkpoint audit and its ordered UI reset live in
[v0.0.2 Runtime UX Recovery Plan](./v0.0.2-runtime-ux-recovery-plan.md). That
plan is the binding response to current runtime findings for Gates 2–5.

## Release promise

Dez v0.0.2 helps a terminal-native developer supervise a long-running coding
agent without losing the terminal that owns the process or the editor context
needed to review its work.

The release-defining workflow is:

1. Open a project.
2. Start a hosted terminal session and run an agent or another long-lived
   command.
3. Move between terminal, editor, search, Git, and agent review without losing
   session identity.
4. Close and reopen the Dez GUI.
5. Reconnect to the same hosted process and terminal history.
6. Inspect changed files and diffs in the editor.
7. Recover safely when the host, shell, project, or credentials are unavailable.

The IDE is not a separate product beside the terminal supervisor. It is the
inspection and intervention surface around the durable terminal: files,
language intelligence, search, Git, diffs, diagnostics, and agent context all
operate on the same project and session.

## Release baseline

| Field                             | v0.0.2 value                               |
| --------------------------------- | ------------------------------------------ |
| Product version                   | `0.0.2`                                    |
| Primary release platform          | macOS on Apple silicon                     |
| Dez source checkpoint             | `4cbe99da2263e781f7aa8725e4dc67ea3d05afc3` |
| Zed merge base before integration | `9d0ef37a25711c00bf6d1ba1142e9de4f4a122a9` |
| Zed source target                 | `b0f145f4aec671970340a528cb8197181e969e8c` |
| Integration merge                 | `6eabc1961e20c684edce44439c95f6d3c22f82a7` |
| Zed stable reference              | `v1.12.0`                                  |
| Integration branch                | `agent/v0.0.2-upstream-parity`             |

The integration incorporates 46 upstream commits. Four textual conflicts were
resolved:

- Preserve Dez identity and the `dez` binary while setting the package version
  to `0.0.2`.
- Preserve the breadcrumb-symbol preference while accepting upstream's safer
  singleton-buffer filename fallback.
- Accept upstream process-group termination for ordinary terminals while
  preserving detach-on-GUI-exit for hosted terminals.
- Regenerate the lockfile with the missing internal dependency edges.

## Evidence vocabulary

Every status claim must use one of these labels:

- **Source**: implemented and inspected in source.
- **Compiled**: the relevant target compiled.
- **Runtime**: exercised in the current Dez binary with recorded observations.
- **Released**: present in a signed, checksummed, downloadable artifact.

Passing a lower evidence level never implies a higher one.

## Current evidence

As of 2026-07-25:

- **Source:** all upstream conflicts are resolved and the merge has no conflict
  markers.
- **Source:** the parity train is a two-parent merge with zero outstanding
  commits from its recorded `upstream/main` target.
- **Packaged:** GitHub Actions
  [run 30112409817](https://github.com/Oreoro/dez/actions/runs/30112409817)
  completed the Apple-M2-targeted stripped release build and launchable DMG at
  checkpoint `91a1514f0236b7d63c946431b08a00114e41f4d4`. The workflow mounted
  the DMG, checked the stable `dev.dez.Dez` identity, verified the application,
  CLI, terminal Host, and Git helper as arm64 executables, and passed strict
  deep signature verification before upload.
- **Compiled:** format, identity, locked offline metadata, shell syntax, merge
  marker, and diff checks pass.
- **Compiled:** the terminal recovery pass replaces the blank
  terminal-plus-overlay state with one top-aligned recovery surface, keeps
  normal tab status compact, and preserves setup actions without crowding the
  default-width Sessions region. Terminal service startup, reconnection, and
  failure now keep transport errors and experimental configuration out of the
  primary UI while retaining explicit local-log and copy-diagnostics actions.
- **Compiled:** failed Workspace restoration now distinguishes one or many
  unavailable entries, exposes the actual Recent Workspaces retry path, and
  names removal without implying that recent Workspace data is deleted. Its
  actions wrap at narrow Sessions widths.
- **Compiled:** Lumin Light no longer renders active controls as dark text on a
  dark background. Its UI accents, focus borders, semantic states, terminal
  ANSI colors, line numbers, and complete syntax palette now meet a checked
  4.5:1 foreground contrast floor on both white and off-white light surfaces.
  A repository guard also verifies Lumin registration and the bundled
  JetBrains Mono editor, terminal, and Markdown-code defaults.
- **Source:** Lumin Blur and its opaque fallback now apply the same readable
  floor to UI text, terminal ANSI colors, and every syntax foreground across
  representative dark translucent and opaque surfaces. Pane focus borders meet
  a 3:1 non-text contrast floor. The guard composites translucent control
  states before checking their text. This dark-theme repair is newer than the
  compiled checkpoint.
- **Source:** all three Lumin variants now have measured region hierarchy.
  Rail/drawer, Main Work Area, tab strip/active tab, terminal, and elevated
  surfaces no longer collapse onto one transparent sheet. Neutral structural
  boundaries clear a 1.5:1 floor, while focus retains its stronger 3:1 floor.
- **Source:** Lumin Blur and Lumin Light now enforce a whole-window glass
  contract. Shell, rail, editor, terminal, toolbar, tab strip, title bar, and
  status bar retain bounded translucency over dark, neutral, cool, and warm
  desktop backdrops; overlays remain denser for legibility. JetBrains Mono now
  supplies the interface as well as editor, terminal, prompt, and review roles,
  making the installed product identity visible outside code. Existing profiles
  with the exact old Dez-generated `.ZedSans` signature migrate in memory and
  receive the normal backed-up update path; custom and official Zed settings do
  not.
- **Source:** first-party action icons now follow one semantic grammar across
  Welcome, Onboarding, Sessions, and terminal context. Terminal,
  Workspace/Files, File, Diff, Session Details, supervision, History, and
  Settings retain distinct object-specific symbols. Agent creation uses Robot,
  the Agent registry uses Blocks, and editor, terminal, diagnostics, Git
  review, and conflict Inline Assist use Sparkle. Generic plus, contradictory
  details icons, and inherited Zed Assistant marks are removed from Dez routes
  while official Zed retains its branded icons.
- **Source:** the built-in file/folder icon set now appears as **Dez
  (Default)** in product settings. **Zed (Default)** remains a registered
  compatibility alias, so existing preferences and the upstream product branch
  do not break.
  The guard composites every translucent layer over representative backdrops.
  This visual-system repair is newer than the compiled checkpoint.
- **Source:** Lumin interaction states now follow one measured ladder across
  blurred dark, opaque dark, and light variants. Hover is visible; active and
  selected are stronger; scrollbars strengthen from idle to hover to drag; and
  the active editor line is never transparent. The guard checks those floors
  and ordering after compositing. This interaction-state repair is newer than
  the compiled checkpoint.
- **Source:** simultaneous Workspace and terminal-service recovery notices now
  share one labeled, scrollable Sessions notice region capped below half the
  viewport height, preserving the primary session list. The empty Main Work
  Area and unavailable-terminal surfaces also scroll vertically with bottom
  breathing room instead of clipping their primary actions in short windows.
  This responsive recovery repair is newer than the compiled checkpoint.
- **Source:** running Agent Sessions now expose **Stop Agent Run** consistently
  inside the Session, from its Sessions row, and from its context menu. The
  in-session icon has an explicit accessible name and retains its Escape
  shortcut hint. The context menu no longer bypasses the selected-row lifecycle
  guard by offering archive during an active run. Agent History now consumes
  that same live status, shows running and permission-blocked state, and
  withholds its icon and keyboard archive paths until completion or failure.
  Permanent deletion likewise requires an archived, inactive record before and
  after its confirmation, preventing a keyboard-only or confirmation-race
  bypass. History archive requests now use the Sessions lifecycle's full
  Workspace/worktree lifecycle. Its row, restore, toolbar, search, and recovery
  controls also participate in the keyboard tab order with explicit accessible
  names. The History collection now announces row position and total size, and
  its named empty/no-results recovery region scrolls instead of clipping in a
  short rail. Its count-only toolbar is now a compact **Agent History · N**
  header with a keyboard-focusable **Back to Sessions** control. Completed and
  failed Sessions retain immediate reversible archive. The Sessions footer
  utilities that own this navigation now participate in the tab order, and
  Agent Tools plus Workspaces report and visibly reflect their open popovers.
  This interaction-parity repair is newer than the compiled checkpoint.
- **Source:** Terminal Sessions now share the Sessions complete rename
  interaction instead of silently ignoring the selected-row command. At normal
  widths, hover or keyboard selection reveals a focusable pencil; every width
  retains context-menu and **Session Rail: Rename Selected Session** entry
  points. Inline edits update Workspace- and Agent-owned terminal Surfaces
  immediately, persist for detached Host-owned Sessions, and leave live shell
  or agent status prefixes authoritative. Blank input or the unmodified live
  title clears the override. Compact rows suppress the extra pencil to preserve
  lifecycle-action space. This source-only hierarchy repair is newer than the
  packaged checkpoint.
- **Source:** the installed-runtime recovery has started with a concrete product
  loop. The Dez welcome screen now teaches **Run → Supervise → Review**, makes
  **Open Workspace** the primary entry, and names a home-directory shell
  **Open Scratch Terminal**. Git's competing **History** label is now
  **Git History**, paired with **Git Changes**. Terminal ownership is also
  truthful: **Persistent** requires the active external terminal Host and its
  matching saved snapshot; GUI-owned computation is a **Workspace Terminal
  Session**. Official Zed retains its upstream presentation. This source-only
  recovery slice is newer than the packaged checkpoint.
- **Source:** pathless Scratch Terminal Sessions now receive a transient empty
  Workspace group in Sessions. This closes the installed contradiction
  where the Main Work Area showed a live shell while the rail said **No sessions
  yet**. Graceful shutdown also gives concurrent PTY cleanup and persistence
  writes two seconds rather than racing the aggregate against 200 ms. Both
  changes are newer than the packaged checkpoint and still require artifact
  runtime verification.
- **Source:** Sessions now distinguishes restoration from a genuinely empty
  product state. Before Workspace restoration reaches ready, the overview and
  empty surface say **Loading sessions** and explain that Dez is reconciling
  Workspaces and saved Terminal Sessions. Start-fresh terminal actions are
  withheld until that state resolves. This recovery-state repair is newer than
  the packaged checkpoint.
- **Source:** missing saved Agent Session tab metadata is now a typed,
  recoverable restore condition. Dez omits the stale tab, presents one explicit
  notice directing the user to Agent History for recoverable sessions, and lets
  the next Workspace serialization heal the saved layout. Other deserialization
  failures remain errors. This restore repair is newer than the packaged
  checkpoint.
- **Source:** standalone Dez terminals now carry their own compact IDE handoff.
  The context bar combines authoritative terminal lifecycle, Workspace/branch,
  and Git changed-file count, then exposes **Files**, **Review Changes**, and
  **Session Details** without parsing terminal prose. Sessions terminal rows
  use the same review/details vocabulary. Newly created Workspace Tools and
  Agent panes start at no more than 360 px or 22% of available width. The same
  constraint now repairs pointer and keyboard resizing, visibility changes,
  layout recipes, and persisted layouts, so the visible contextual drawer
  preserves at least 60% for the Main Work Area. Restored Agent panes also
  retain their Agent-region identity instead of decoding as ordinary tabs. This
  terminal-to-IDE and layout repair is newer than the packaged checkpoint.
- **Source:** Workspace Tools and Agent now behave as one optional drawer at
  every window size. Revealing either hides the competing drawer; restored
  double-drawer layouts collapse toward the focused or recipe-appropriate
  region. Extra ultrawide space remains available to the Main Work Area rather
  than creating another persistent tool column. The terminal handoff is a 32 px
  tab-bar-aligned header without a redundant visible actor title, and the
  rail's visible heading is the plain **Sessions**. This structural hierarchy
  repair is newer than the packaged checkpoint.
- **Source:** Full, Agent Control, and Editor Focus now converge on one
  authoritative Main Work Area instead of leaving a blank sibling from an
  earlier split recipe. Cleanup hides only surplus empty tab panes; a pane with
  user content remains visible even when that exceeds the recipe target.
  Normal Workspace restoration now invokes the same cleanup for the default
  layout and saved single-work-area recipes, while explicit multi-pane recipes
  keep their requested empty panes.
  Active-pane context now uses the title and selected tab by default instead of
  a two-pixel accent rectangle around the entire work surface. Dez rejects that
  whole-pane focus overlay at render time even when imported settings request
  it; official Zed retains the configurable upstream border. The empty Main
  Work Area is now one top-anchored native launch
  region headed **Run, supervise, and review in this Workspace** rather than
  the generic **Workspace ready** placeholder or a floating surface card. Its
  passive route states Terminal in the Main Work Area, live state in Sessions,
  and review through Files, Git, and diffs without resembling another action
  row. The true-empty Sessions rail repeats that same route as a narrow,
  accessible icon-and-copy list with no borders or control backgrounds before
  its Open Workspace and scratch-terminal actions. This blank-pane,
  product-clarity, and focus-hierarchy repair is newer than the packaged
  checkpoint.
- **Source:** Main Work Area notifications are now a responsive, named shelf
  measured from the actual work surface rather than a fixed 448 px,
  full-height overlay. Width stays within the surface, height stops at 36%,
  the shelf is top anchored below the toolbar/tab area, and overflow scrolls.
  Toasts no longer reserve an invisible full-screen interaction layer; visible
  toasts are capped at 560 px and 30% of viewport height with status-bar
  clearance. Empty drawers now return to the Main Work Area with inward arrows;
  their recovery content owns the full drawer height, stays top-anchored, and
  scrolls internally instead of floating at the parent's center. Welcome
  presents Run, Supervise, and Review as one native icon-and-copy list rather
  than three competing cards, a floating panel, or numbered selected-looking
  pills. Its headline separately states the Workspace-to-IDE promise instead
  of repeating the list. This transient-layer and recovery-surface repair is
  newer than the packaged checkpoint.
- **Source:** the terminal context strip clips its shrinkable
  lifecycle/repository metadata and progressively labels its fixed actions.
  Ordinary split widths show one primary handoff—Review when changes exist,
  otherwise Files or Open Workspace—then add secondary labels only at 720 px
  and 920 px. Every compact icon retains its accessible name and tooltip, and
  full metadata remains in Session Details. This compact-width repair is newer
  than the packaged checkpoint.
- **Source:** the welcome screen is now state-aware. The empty application leads
  with **Open Workspace** and **Open Scratch Terminal**; an already-open
  Workspace instead leads with **Start Terminal Session** and **Open Files**.
  The workflow explanation also changes from opening a codebase to acting in
  the current one. Meaningfully titled Dez terminal tabs now render as
  **Terminal · title**, while official Zed retains its upstream labels. This
  onboarding and surface-vocabulary repair is newer than the packaged
  checkpoint.
- **Source:** that state-aware Welcome now gives its first useful transition
  the only filled treatment: **Open Workspace** when empty or **Start Terminal
  Session** inside a Workspace. Git, Debug, Outline, and Agent icon toolbars
  now expose their visible controls in the keyboard tab order with specific
  accessible names. Dez Debug creation/termination use Debug and Stop icons,
  and Agent title editing no longer depends on pointer hover. Official Zed
  keeps its upstream hierarchy and symbols. This source-only focus and action
  hierarchy repair is newer than the packaged checkpoint.
- **Source:** Welcome's primary action list is now the planned three-step
  choice rather than five competing entries. Empty Dez offers Open Workspace,
  Clone Repository, and Open Scratch Terminal; a ready Workspace offers Start
  Terminal Session, Open Files, and New File. The empty Main Work Area uses the
  same Start Terminal Session wording and no longer calls a default GUI-owned
  terminal durable. Static guards protect count, order, destination, and truth.
  This source-only start-hierarchy repair is newer than the packaged checkpoint.
- **Source:** Sessions selection now has a complete IDE handoff on every
  supported desktop keymap. `Shift+Enter` returns to the existing selected
  Session, `Shift+F` opens its Workspace files, `Shift+G` opens Agent review or
  Git Changes according to Session ownership, and `Shift+V` opens evidence-backed
  Session details. The corresponding Command Palette entries use Sessions
  vocabulary. All four routes activate the selected owner instead of creating
  another terminal or project context. This keyboard and discoverability repair
  is newer than the packaged checkpoint.
- **Source:** `Shift+F` and the selected Session **Files** action now also
  handle a closed owning Workspace. Dez reopens that exact Workspace, restores
  the selected Agent or Terminal Session, and then reveals Files. The route no
  longer fails silently and never creates a substitute Session. This
  restoration-sequencing repair is newer than the packaged checkpoint.
- **Source:** Terminal **Review Changes** now performs the complete review
  transition. One Workspace-owned action reveals Git Changes, selects an actual
  changed file, and opens the uncommitted diff in the Main Work Area. It no
  longer composes two toggle actions that could close the drawer or leave the
  user staring at a list with no diff. Clean repositories retain their explicit
  clean state. This review-flow repair is newer than the packaged checkpoint.
- **Source:** that Main Work Area review now names its active destination
  **Diff · filename** instead of the generic inherited **Uncommitted Diff**.
  Hover detail retains the diff base and relative path, and official Zed keeps
  its inherited title. This Surface-identity repair is newer than the packaged
  checkpoint.
- **Source:** Welcome, Terminal, and Sessions **Files** handoffs now
  dispatch one Workspace-owned, idempotent reveal/focus action. Repeating the
  welcome button, terminal button, context command, or selected-session
  keyboard route keeps Files open instead of toggling it closed or exposing a
  previously active tool. The Command Palette names the destination **files:
  open**. Welcome and the empty Sessions state also use the folder-only,
  same-window **Open Workspace** route. While App Session restoration is
  pending, the rail remains in **Loading sessions** and withholds start-fresh
  actions. A true-empty rail names **Open Scratch Terminal** explicitly. This
  navigation and zero-state repair is newer than the packaged checkpoint.
- **Source:** the true-empty Sessions state now matches Welcome's progressive
  activation loop. **Open Workspace** is the single filled action;
  **Open Scratch Terminal** remains an outlined pathless alternative. Once a
  Workspace exists but no Session does, **Start Terminal Session** becomes the
  filled recovery action. Restoration uses its own loading icon and suppresses
  stale attention styling. This source-only state hierarchy repair is newer
  than the packaged checkpoint.
- **Source:** Sessions Workspace controls now follow keyboard focus as well
  as pointer hover. A focused Workspace keeps Start Terminal Session and Options
  visible;
  both controls enter the tab order with Workspace-specific accessible names.
  The already-open Options menu no longer hides its worktree close action until
  hover. Session-search clearing and onboarding-banner dismissal are also
  keyboard-focusable. Workspace names now always truncate inside a flexible
  content allocation beside a fixed action cluster; the former opaque-window
  gradient masks are gone, matching the Session-row no-overlay contract. An
  expanded empty Workspace now shows only its labeled Start Terminal Session
  action; the redundant header terminal icon returns only when the Workspace is
  collapsed or populated. Its decorative **Ready for a session** caption is
  also gone because the overview and accessible Workspace header already carry
  that state. This source-only interaction-parity repair is newer than the
  packaged checkpoint.
- **Source:** Main Work Area pane chrome now applies that keyboard contract to
  Back, Forward, Add, Switch Surface, Split, Zoom, Hide Workspace Tools, and
  Hide Agent. The active unpinned Dez Surface keeps Close visible and
  keyboard-focusable even under the hover-close preference; inactive tabs stay
  quiet, while pinned dirty tabs retain their status indicator. Official Zed
  keeps its upstream tab-close behavior. This source-only pane-chrome repair is
  newer than the packaged checkpoint.
- **Source:** the typography/theme upgrade path now recognizes the actual older
  generated Dez profile found in the installed configuration. That exact
  signature pinned `.ZedSans`, light mode, and One Light despite describing
  Lumin system behavior. It now migrates those generated values together to
  JetBrains Mono, system appearance, Lumin Light, and Lumin Blur. Official Zed
  and arbitrary custom font/theme profiles remain untouched. The current local
  generated profile was aligned directly without building or launching Dez.
- **Source:** Sessions creation emphasis now matches lifecycle state. An
  empty ready Workspace exposes a filled, Workspace-named **Start Terminal
  Session** action. After at least one Session exists, each Workspace header
  retains its compact scoped terminal action while the Dez overview remains
  dedicated to status and All/Attention scope. Official Zed retains its
  compatibility overview action. This source-only action-ownership repair is
  newer than the packaged checkpoint.
- **Source:** terminal-focused Workspace status no longer expands routine
  Search and zero-diagnostics into **Search files** and **No diagnostics**
  labels. Both remain named, keyboard-focusable controls with action tooltips;
  diagnostics still exposes visible errors, warnings, counts, and messages.
  This source-only persistent-chrome reduction is newer than the packaged
  checkpoint.
- **Source:** **Restore Dez Visual Profile** now gives imported or customized
  installations an explicit route back to system-selected Lumin Light/Blur,
  built-in Dez icons, and JetBrains Mono across UI, editor, terminal, and
  Markdown code. It preserves sizes and non-visual settings and reports success
  only after persistence. This recovery action is newer than the packaged
  checkpoint.
- **Source:** the active Lumin material and JetBrains UI role now cross
  secondary-window boundaries. About and Copilot verification no longer use
  GPUI's opaque/default-font fallbacks; Audio Test now initializes the same UI
  font as its already theme-aware shell. Intentionally shaped collaboration
  notification popups retain transparent windows while using the configured UI
  font. Agent attention stays in Sessions by default; its shaped popup uses the
  same material only when **Floating Attention Popups** is explicitly enabled.
  Official Zed retains its inherited popup policy. Static guards protect the
  distinction. This source-only repair is newer than the packaged checkpoint.
- **Source:** **Reset Pane Sizes** no longer recreates equal-width Git/tool and
  terminal columns. It recursively resets ordinary editor splits, then
  reapplies the Dez auxiliary-pane ceiling so the Main Work Area retains its
  hierarchy. This reset-path repair is newer than the packaged checkpoint.
- **Source:** every primary focus root implicated by the installed audit now
  exposes a stable accessibility node. Files in both populated and empty state,
  Outline, Git Changes/History, Debug, Agent, native and external Agent
  Sessions, Agent History, and terminal failure/panel surfaces pair their
  tracked focus handle with an ID, semantic role, and product-specific label.
  This closes the known source omissions but does not claim the runtime log is
  clean until the rebuilt artifact is exercised with accessibility enabled.
- **Source:** once a terminal exists, the product model remains available under
  **Session Details** as **How Dez Works**: run in the Terminal Session,
  supervise it in Sessions, and review its Workspace through Files and Git.
  This orientation repair adds no permanent chrome and is newer than the
  packaged checkpoint.
- **Source:** **Session Details** now discloses the evidence contract beside
  those facts. Terminal/Host owns lifecycle, Git counts remain Workspace-owned,
  Session attribution is not inferred, agent confidence/checks require trusted
  evidence, and terminal text is not proof. This trust-boundary repair is newer
  than the packaged checkpoint.
- **Source:** Scratch Terminals no longer end at a details-only context strip.
  **Open Workspace** uses a folder-only picker, adds every chosen codebase to
  the current window, and keeps the running Terminal Session; then the normal
  Files and Git review actions take over. The guide truthfully says **Connect**
  before that transition. This handoff repair is newer than the packaged
  checkpoint.
- **Source:** the native Dez shell now has a single Lumin material hierarchy.
  macOS windows use the native under-window backdrop, blend behind the window,
  and follow active/inactive system state. Sessions and Workspace Tools paint
  panel material; the Main Work Area keeps editor/terminal material; empty work
  cards use surface material. Lumin Light controls are checked translucent
  interaction layers rather than opaque beige blocks. Workspace notifications
  and toasts use bounded, non-modal glass feedback with lighter elevation and no
  modal shadow. The View and File menus now use visible **Sessions** and **Start
  Terminal Session** Dez copy while retaining official-Zed compatibility
  branches. This native-material and product-vocabulary repair is newer than the
  packaged checkpoint.
- **Runtime:** no claim is made for the packaged checkpoint. GitHub verified
  the application structure but did not launch its UI, and the exact downloaded
  artifact has not yet passed the local smoke matrix.
- **Released:** no public v0.0.2 release exists. The retained GitHub Actions
  development artifact is ad-hoc signed, not notarized, and expires on
  2026-08-07.

The latest local raw executables predate the GitHub build:

| Artifact                         | Size | SHA-256                                                            |
| -------------------------------- | ---- | ------------------------------------------------------------------ |
| `target/debug/dez`               | 1.0G | `08e1419ca1b25d1be7441b1ee7063e4c6f84c6805df397ee643061c89ba60938` |
| `target/debug/dez-terminal-host` | 18M  | `35acd45896a00bdf3be8a40fd1e6daf626ca584a3e60e6c905fa0cbd67cc6c0d` |

The historical GitHub workflow run #2 uploaded the unsigned 224,161,629-byte
artifact `dez-macos-arm64-3164618199fc5bf1603b91665e81f7b9fdf0ddea`
with SHA-256
`39454d4e8cc30e382f14f5b90b85bedb2b2421805d9e99d20fc11e3734ac8791`.
The workflow verified both contained executables as arm64 Mach-O files before
packaging. That artifact was a raw executable tarball, not a macOS application
bundle, so Finder could not install or open it as Dez. It remains historical
compile evidence only and is superseded by the launchable-DMG workflow.

Run 30112409817 uploaded the 144,521,832-byte artifact
[`Dez-macOS-M2-91a1514f…`](https://github.com/Oreoro/dez/actions/runs/30112409817/artifacts/8605712058).
It contains `Dez-macOS-M2.dmg`, a build manifest, `SHA256SUMS.txt`, and
`INSTALL.txt`. Artifact-level recompression is disabled because the DMG is
already compressed. This is installable development evidence, not a notarized
public release.

## Ordered release gates

### Gate 0: upstream parity and identity

- Complete a two-parent merge of the recorded Zed target.
- Keep `Dez`, `dez`, the Dez bundle identifiers, updater identity, and terminal
  host protocol distinct from Zed.
- Record every conflict and deliberate divergence.
- Make future upstream synchronization repeatable.

### Gate 1: reproducible build

- Build the `dez` application, CLI, and `dez-terminal-host` from the locked
  workspace.
- Use the manual `Build Dez for macOS` workflow when local storage cannot hold
  a clean build. It runs on GitHub's arm64 `macos-15` image and targets this
  development machine's Apple M2 CPU with the stripped thin-LTO release
  profile. It creates the stable-identity `Dez.app`, packages the application,
  CLI, terminal Host, Git helper, and resources in a DMG, verifies the arm64
  inventory and deep signature, and uploads the DMG with a manifest, checksum,
  and installation guide.
- Treat this workflow output as an ad-hoc-signed development snapshot. It is
  not Developer ID signed, notarized, or a public release.
- Run formatting, identity, shell syntax, metadata, and diff checks.
- Record artifact sizes, types, and SHA-256 checksums. Runtime remains unproven
  until the exact downloaded `Dez.app` opens and passes the smoke matrix.

### Gate 2: durable terminal contract

- Starting a hosted terminal creates one visible session with a stable identity.
- Closing the GUI detaches instead of terminating its hosted process.
- Reopening the GUI reconnects to the same process and restores useful history.
- Explicit stop terminates the hosted process group and produces an honest final
  state.
- Ordinary terminals retain upstream close and process-group cleanup behavior.

### Gate 3: coherent IDE integration

- Opening a session selects the related terminal and project context.
- Editor, search, Git, diagnostics, and diffs remain normal first-class IDE
  tools.
- Agent output can lead to the exact changed file or diff without creating a
  second navigation model.
- Pane and panel actions use predictable focus, close, split, and restore rules.

### Gate 4: failure and recovery

- Missing host, disconnected host, exited process, invalid working directory,
  and authentication failure each have distinct copy and recovery actions.
- Retry is safe and never creates an invisible duplicate process.
- A failed terminal termination remains visible, names the possibility that the
  process is still running, and offers **Return to Session** for the exact
  owning Workspace/source. It does not auto-hide or expose transport/Host
  jargon; detailed errors remain in logs.
- Destructive actions identify the target and state what will happen.
- Empty and loading states never resemble a broken blank window.

### Gate 5: visual, input, and accessibility quality

The following matrix must be inspected at compact, default, and wide window
sizes:

| Surface          | Required states                                                 |
| ---------------- | --------------------------------------------------------------- |
| Sessions         | empty, populated, attention, disconnected, search, overflow     |
| Terminal Session | running, idle, command active, exited, detached, reconnecting   |
| Editor           | file, diff, diagnostics, preview tab, split                     |
| Agent review     | streaming, complete, tool activity, error, changed files        |
| Recovery         | missing host, stale session, invalid project, retry in progress |

Each state must be usable with keyboard and pointer, expose visible focus, retain
legible contrast in Lumin Light and Lumin Blur, respect reduced motion, and
present meaningful labels to assistive technology. JetBrains Mono remains the
interface, terminal, and code default where available; all surfaces must use
the design tokens rather than ad hoc font sizes.

### Gate 6: security and trust

- Hosted-process ownership and cleanup are explicit.
- Secrets are not written to session metadata, logs, or recovery copy.
- Agent and terminal actions reveal their working directory and trust boundary.
- Risky actions require a clear confirmation appropriate to their impact.
- Any upstream sandboxing change is audited and documented before release.

### Gate 7: preview release

- Package and sign the macOS Apple-silicon application and helper.
- Verify clean install, upgrade, relaunch, reconnect, and removal paths.
- Publish checksums, known limitations, recovery semantics, and rollback steps.
- Promote only after the hero workflow passes from a clean installation.

## Next ten tasks

1. Finish the remaining source-only interaction audit across Sessions,
   Main Work Area, Workspace Tools, and Agent without starting another build.
2. Audit the inherited sandboxing change and record the explicit v0.0.2
   treatment in the upstream ledger.
3. Freeze one source checkpoint after formatting, identity, locked metadata,
   documentation, and diff checks all pass.
4. Build and package that exact checkpoint for macOS Apple silicon; do not use
   the older installed artifact as evidence for newer source repairs.
5. Run the hero workflow: open a Workspace, start one Terminal Session, run an
   agent, leave it running, edit beside it, and review its exact changes.
6. Close and reopen Dez, then prove the same hosted process, terminal history,
   Workspace, and Session identity reconnect without a duplicate shell.
7. Exercise missing Host, stale Session, exited process, invalid Workspace, and
   clean/no-repository Git recovery states.
8. Run the compact/default/wide visual matrix in Lumin Blur and Lumin Light,
   including keyboard, pointer, focus, reduced-motion, and accessibility labels.
9. Verify Dez/Zed coexistence and ensure no Zed updater, endpoint, storage,
   bundle, helper, or CLI identity can be selected by Dez.
10. Sign, notarize, checksum, document, and publish only the checkpoint that
    passes the runtime and visual evidence gates.

## Explicitly outside v0.0.2

- A speculative split into new orchestration, event, policy, scheduler, adapter,
  artifact, CLI, or headless-daemon crates.
- Remote execution, shared cloud sessions, mobile or browser clients, and team
  administration.
- A second editor, terminal emulator, Git client, task system, or extension
  platform alongside Zed's existing subsystems.
- A Synara-style external MCP bridge for local clients. The design is useful for
  a later scoped Dez integration, but v0.0.2 must first prove the native
  terminal-to-IDE loop.
- Claims of reliability, accessibility, security, or performance that lack the
  evidence level stated above.

## Latest source checkpoint

The Sessions footer now uses width-aware destination labels: detailed rails
show **Agent Tools**, **Agent History**, and **Recent Workspaces**. The default
compact rail keeps the familiar Settings gear icon-only—with its complete
tooltip and accessible name—while preserving the ambiguous **History** and
**Workspaces** labels. Narrower rails use the same named, tooltip-backed icons
for all three. This removes visible ambiguity and reserves enough width for the
navigation labels. It remains source-only until the next artifact passes the
compact/default/wide visual matrix.

Recognized terminal agents now use provider-specific bundled icons in both the
Sessions and keyboard session switcher through one shared mapping. Unknown
or unrepresented providers retain the neutral Robot fallback. The next artifact
must prove the icons remain legible in Lumin Blur and Lumin Light and do not
replace the authoritative provider and Session text.

The terminal context toolbar now responds before it clips: its actions keep
labels at normal widths and retain semantic icons, complete accessible names,
and matching tooltips in narrow panes. Review Changes now uses the same
Workspace-wide repository scope as its count, selecting the active dirty
repository or the first dirty repository and opening an actual diff. Runtime
acceptance must cover narrow terminals and a clean-active/dirty-secondary
multi-repository Workspace.

Git Changes now gives more vertical measure back to changed-file review: the
collapsed commit composer uses four lines, with full-height and modal expansion
still available. View Diff, stage/unstage, commit, remote, and split-menu
controls are explicit tab stops; popup chevrons announce their destination and
expanded state. The next artifact must verify this compact hierarchy at the
bounded Workspace Tools width.

Agent recovery now obeys the same exact Workspace contract as Welcome and the
Sessions: its primary action accepts a folder and stays in the current Dez
window. Shared Open Workspace and Clone Repository recovery controls are
keyboard tab stops. Agent Options and New Agent Session triggers report
expanded state and retain selected treatment while open. The next artifact
must prove focus visibility, popup state, cancellation, and same-window folder
selection in both Lumin variants.

Agent provider onboarding now names its scope and resulting object. The
reachable Dez path says **Agent provider setup**, **Configure Agent
Providers**, and **Start Agent Session**; it filters the inherited Zed Cloud
provider, uses no native-thread vocabulary, and puts both actions in the
keyboard tab order. The next artifact must prove this path never renders the
dormant Zed AI trial/plan onboarding and that configured-provider state changes
the same card without creating a duplicate Session.

The Agent composer now treats its primary icon row as a real control surface:
size, context, follow, speed, thinking, effort, send/queue, stop, and sandbox
actions are tab stops with specific names. Stateful controls expose toggle,
expanded, and current-value semantics and use selected treatment in addition
to color. The next artifact must prove logical focus order, visible focus,
disabled Send explanation, popup cancellation, and state updates at compact
and default Agent widths.

Agent follow-up interaction now has the same standard. Response actions stay
at readable contrast and expose names, tab stops, and selected feedback state.
Queued prompts are a named ordered list whose row actions do not disappear
without hover. Permission decisions are tab stops and Permission Scope reports
its value, expanded state, and open treatment. The next artifact must verify
ordered queue announcements, exact-row mutation, permission-card ownership,
disabled-Allow recovery, and focus visibility at compact/default widths.

Agent Review now keeps per-file decisions visible and row-specific, explains
pending-edit disablement, and places per-file/review-all actions in the tab
order. Subagent Stop and Return actions name their distinct effects. Restore
Checkpoint requires a Workspace-file replacement warning before applying the
exact checkpoint. The next artifact must prove narrow-row layout, focus order,
exact-file Keep/Reject, parent return without cancellation, confirmation
cancel/accept paths, and restoration of the expected file contents only.

Agent recovery now distinguishes Retry, Configure, Authenticate, Select,
Open, Update, Copy, and Dismiss through named keyboard actions; retry copy uses
the active Dez identity. Shared Copy and skill-file rows expose accessible
names. Data-retention consent requires a warning before persisting the setting
and retrying. The next artifact must prove logical focus order, copied-state
feedback, exact-warning dismissal, recovery destination, Cancel with no
mutation, and one successful confirmed consent-and-retry path.

Agent tool cards now keep copy and disclosure controls visible at rest.
Thinking, command output, general tool output, and Subagent previews expose
named keyboard disclosures and expanded state. Stop Command, sandbox help,
discard interrupted edit, Open File, and Stop Subagent are explicit tab stops.
Truncation and command failure are status, while **Open Subagent Session** is a
labeled navigation action to the existing Session. The next artifact must
prove compact-card layout, logical focus order, exact tool ownership,
status announcements, copy feedback, preview without restart, and navigation
without Session duplication.

The true-empty Sessions state now teaches the integration in place: **Start with
a Workspace** explains that the codebase supplies context to Terminal or Agent
Sessions and that their changes return to the IDE for review. A passive,
individually named list connects Terminal in the Main Work Area, live state in
Sessions, and Files/Git/diff review without pretending those explanations are
buttons. Open Workspace remains primary and same-window; Open Scratch Terminal
remains explicitly pathless and secondary. Start, recovery, Workspace-scoped
creation, and All/Attention scope actions are keyboard tab stops. The next
artifact must prove compact wrapping, logical focus order, correct same-window
folder selection, scratch scope, and the transition from zero state to one
exact Session row.

Sessions recovery notices now put every diagnostic and recovery action in
the keyboard tab order. Terminal Session notices preserve no-shell,
process-untouched, and no-fallback truth. Workspace recovery uses the explicit
**Remove Recovery Entry** label and states that recent Workspace data remains.
The next artifact must prove notice stacking, compact wrapping, Copy feedback,
local-log routing, Recent Workspaces retry, exact-record removal, and no false
reconnect or successful-restore state.

Sessions rows now use ordinary truncation and inline layout instead of gradient
layers that paint across titles when hover actions appear. Terminal rows expose
one primary IDE handoff at a time: **Review** when the owning Workspace has
changes, otherwise **Details**. Agent rows expose one state action: **Stop**,
**Discard**, **Review**, or **Brief**, in that priority order. Normal and
default rail widths keep those labels; only widths below 280 px collapse them
to named icons. Rename, archive, hook setup, and diagnostic utilities stay in
the selected-Session command path or existing context menu, leaving only the
current decision inline. The next artifact must prove bounded title truncation,
no hover patch or clipping, correct state priority, and context-menu reachability
at 240, 280, 300, and 380 px rail widths.

Pane headers now follow the same no-overlay rule. Dez suppresses the inherited
absolute whole-pane drag handle, including its larger invisible pointer target,
so the center of a tab or drawer header cannot be covered by an unexplained
drag affordance. The stable Sessions, Workspace Tools, Main Work Area, and
Agent regions continue to move content through explicit split/move actions,
ordinary Surface tab drag and drop, and Canvas layout recipes. Official Zed
retains its upstream handle. The next artifact must prove header controls remain
unobstructed and that the documented movement paths still work.

The Agent header now applies that rule to its title editor. In Dez, the title is
a flexible, truncating slot and the visible pencil is a fixed inline sibling;
neither the inherited absolute hover layer nor its opaque-window gradient mask
is rendered. Official Zed retains the upstream overlay behavior. The next
artifact must prove long Agent Session and terminal titles truncate before the
control at compact and default Agent widths, including Lumin Blur, and that
keyboard activation opens the correct title editor.

Agent plan rows now use the same allocation model. The collapsed current task
shrinks inside a one-line flexible slot and the remaining-task count occupies a
fixed sibling slot. Expanded rows use ordinary clipping plus their existing
full-text tooltip. Dez renders no right-edge gradient mask in either path;
official Zed retains its upstream presentation. The next artifact must prove
long task text, changing pending counts, disclosure, clear-plan focus, and
tooltip recovery at compact and default Agent widths in both Lumin variants.

The main shell is now deliberately minimal at every width: Sessions, a dominant
Main Work Area, and at most one optional drawer. Workspace Tools and Agent are
always mutually exclusive in Dez, including ultrawide windows and restored
layouts. The active drawer or current layout recipe wins deterministically;
official Zed retains its upstream coexistence behavior. The next artifact must
prove Workspace Tools -> Agent -> Workspace Tools transitions, focus return,
restoration from a saved double-drawer layout, and preserved Main Work Area
width at laptop, desktop, and 2400 px ultrawide sizes.

The bundled launch profile now follows that hierarchy inside each region.
Persistent Back and Forward buttons are hidden from Main Work Area tab bars,
and generic quick actions plus the selection menu are hidden from editor
toolbars. Their shortcuts and commands remain available. Add, split, Surface
switching, breadcrumbs, diagnostics, and Agent review stay visible because
they explain or advance the current work. Sessions defaults to Agent state and
latest attention only; branch, worktree, and layout detail remains available
in the owning Workspace and its options instead of repeating in every Session
row. The next artifact must prove the reduced default at editor, terminal,
diff, and Agent Surfaces, plus recovery of hidden actions by keyboard and
command search.

Command search now uses the same bounded Canvas Layout vocabulary as
**Workspace Options**. Dez exposes Full, Agent Control, Focus Editor,
Code/Run/Observe, Review, and Debug, followed by Cycle, Save Layout As, Manage
Saved Layouts, and Restore Previous. The generic centered-editor toggle, legacy
Classic/Canvas toggles, experimental multi-agent matrices and studios, numeric
slot actions, and clipboard/storage internals stay out of search. The
underlying action types remain available to saved-layout UI and compatibility
paths. The next artifact must prove the Canvas Layout command family contains
only these ten understandable routes and that every visible entry performs the
named transition.

The Main Work Area terminal projection now follows foreground-process changes
instead of waiting for a tab to be added, removed, or reselected. An existing
shell that starts Codex emits a semantic process-info event after its
asynchronous refresh; Sessions subscribes to existing and future terminal
Surfaces and updates the same row. Raw PTY output wakeups do not rebuild
Sessions or Host metadata. This keeps shell → Codex → shell within one terminal
and one Session identity while allowing its provider and lifecycle presentation
to change. When stored metadata and the live Main Work Area terminal describe
the same identity, the live terminal now owns the row and its activation route.
Clicking or confirming that Session focuses the exact existing terminal instead
of attempting a Host reattach or producing a duplicate Surface. Stale stored
paths cannot move a live row under the wrong Workspace.

Host-owned terminals now participate in that same transition instead of
stopping at display-only terminal state. The terminal helper samples the PTY
foreground process group for a short bounded window around input, normalizes
the command, and publishes it in the durable Session snapshot. The GUI mirrors
that host evidence into the attached terminal and Sessions, so starting plain
`codex` changes the existing row to **Codex · Running** without requiring a
hook or scraping TUI output. Structured attention and review evidence still
require an adapter. Selecting a detached Host Session now marks its row
**Opening…** before asynchronous attachment begins and coalesces repeated
clicks; completion focuses one restored Surface. This is source-only and still
requires runtime proof with the rebuilt app/helper pair.

The same process transition now refreshes the existing terminal Surface, not
only Sessions and its tab owner. Recognized foreground agents receive one quiet
acknowledgement in existing chrome: the terminal tab adopts the provider icon,
while the compact context strip says **Codex running**, **Claude Code running**,
or the corresponding supported agent without repeating that icon. No panel
opens and the TUI remains the Main Work Area content. An ordinary active shell
adds no generic **Active** label; task failures and other meaningful states
remain visible. Official Zed retains its upstream behavior. The detail tooltip
identifies foreground-process evidence rather than attributing Git changes to
the agent. Below 360 px the tab icon remains while the visible activity label
yields to the terminal actions; the complete state stays in the toolbar
accessibility label and details.

That response is now equally explicit inside responsive Sessions. At compact
widths, where supplemental actor metadata intentionally yields, the row
combines actor and state as **Codex · Running** rather than rendering a generic
terminal title beside **Detected · Live**. A collapsed Workspace header shows
the single running provider or a count of running terminal Agent Sessions, and
its attention total includes terminal agents. The group stays collapsed and
terminal focus is preserved, so the acknowledgement cannot become a layout
jump, panel, toast, or overlay. The next artifact must prove shell → Codex →
shell in expanded and collapsed groups and confirm that clicking the updated
row focuses the original terminal without a second Session.

Sessions also has one responsive width contract shared by the outer workspace
reservation and the inner rendered rail. Compact mode remains 280 px on wide
windows, becomes 240 px at an 800 px viewport, and uses a 200 px lower bound in
the narrowest supported window. Dez now enforces a 600 × 400 px native window
floor, so that 200 px rail still leaves at least 400 px for the Main Work Area
before an optional drawer. The inherited 360 px floor would leave only 160 px
for primary work and is not a valid Dez layout; official Zed retains 360 ×
240 px. The rail remains a normal flex sibling of the Main Work Area; it is not
a floating or overlapping terminal sidebar. Its
client-decoration root now remains in normal flow inside that exact
reservation, so painting and pointer hit testing share one geometry owner. The
header divider is applied to the header itself rather than an absolute
full-header child. This source slice has formatting, metadata, identity, theme,
and diff evidence only. The next artifact must prove the two process
transitions, no duplicate row, sustained terminal-output responsiveness, and
the 600/800/1400 px width matrix.

Dez session switching is now direct navigation rather than a transient mode.
While Agent or Sessions has focus, the switch-session action activates the next
or previous visible Session through its owning Agent, center-terminal, or Host
route. It mounts no dialog or full-window interaction boundary and needs no
preview confirmation. Main Work Area `Ctrl-Tab` remains conventional Surface
switching. Official Zed retains the inherited modal switcher and its
source-preserving preview restoration.

The narrow interaction hierarchy now follows that geometry. At the 200 px
Sessions floor, scope controls say **All** and **Needs** with counts and compact
targets; the complete status remains available by tooltip and accessibility
name. At 240 px and above, **Attention** returns. One unfiltered Session omits
the entire scope row; an active Attention projection retains it so the filter
always has an exit. The one optional Workspace Tools or Agent drawer now uses
a bounded usable-width target rather than a fixed 22% at every size. Search is
not a permanent third row: one named overview control appears when multiple
Sessions make filtering useful, while the existing keyboard action remains
available at every count. Either reveals and focuses the inline field; close
clears it and returns focus to Sessions. The drawer targets 240 px when space
permits, 360 px maximum, and never more than 40%, preserving at least 60% for
the Main Work Area. The next artifact must prove scope labels, search
disclosure, tool content, resizing, and focus at the 200/240 px rail and
400/560/1000/2000 px workspace budgets.

Sessions visibility now follows the same single-owner rule as the rest of the
shell. Fresh windows begin with the projection closed, while restored windows
may keep the user's saved open state. An open rail owns its **Hide Sessions**
action in the overview instead of duplicating it in generic window chrome; a
closed rail remains recoverable through **Open Sessions** in existing
title/status chrome. Compact mode presents Agent Tools, Agent History, and
Recent Workspaces as icon-only utilities with complete tooltip and accessible
names, and detailed mode restores their visible labels. The next runtime
artifact must prove open → hide → reopen, keyboard focus recovery, and compact
versus detailed footer behavior without overlap.

The fresh Workspace now begins with the Main Work Area as its only full-height
region. Workspace Tools no longer opens merely because a folder is available;
Files, Outline, Git, and Debug reveal it on demand, and the explicit
`project_panel.starts_open` preference remains the opt-in. Agent and Sessions
already begin closed. Restored windows keep intentional state, then pass
through the one-drawer exclusivity and 60% Main Work Area budget. The next
runtime artifact must distinguish fresh-default behavior from restored and
user-overridden behavior.

Transient status feedback now respects the same responsive ownership rule.
Long copy shrinks and truncates within the bounded toast, keeps its complete
text in a tooltip, and cannot displace the fixed-width action or dismiss
controls. Both controls are keyboard tab stops with explicit accessible names.
The next runtime artifact must prove a long message with both controls at 600,
800, and 1400 px without clipping or an invisible pointer-blocking layer.

Persistent status chrome now applies that priority before rendering. A closed
Sessions projection keeps **Open Sessions** in a fixed edge allocation, while
Search, language-server, diagnostics, activity, and other optional status items
shrink and clip inside a separate region. The next runtime artifact must prove
that both left- and right-positioned Sessions remain recoverable when editor
status content exceeds the 400 px Main Work Area budget.

Sessions now has a strict admission rule: only work that can be supervised
appears. Empty Workspaces and empty Agent composers no longer create rows or
inflate the Session count. The zero state offers one terminal-first action for
the active Main Work Area; Workspace navigation stays in Workspace Tools and
Recent Workspaces. With no Workspace, the rail presents only open Workspace and
scratch Terminal rather than a permanent Run/Supervise/Review tutorial. Once a
terminal exists, its stable row reacts to semantic foreground-process changes,
so launching Codex changes the existing row to **Codex · Running** without
opening a drawer, popup, or duplicate terminal. Auxiliary-pane dismissal also
recovers focus from the pane that actually owned it rather than trusting a
potentially stale active-pane marker. The next runtime artifact must prove
empty → terminal → Codex → shell, row selection back to the exact terminal,
and keyboard input after closing each auxiliary pane.
